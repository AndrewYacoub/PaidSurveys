<style>
  .product-details, .survey-details {
      margin-bottom: 20px;
    }
    .question-card {
      margin-bottom: 20px;
    }
    .btn-primary {
      background-color: #28a745;
      border-color: #28a745;
    }
    .btn-primary:hover {
      background-color: #218838;
      border-color: #1e7e34;
    }
  </style>
<div class="container mt-4">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><a href="<%= root_path %>">Home</a></li>
      <li class="breadcrumb-item"><a href="<%= category_product_path(@category, @product) %>"><%= @product.name %></a></li>
      <li class="breadcrumb-item active" aria-current="page"><%= @survey.title %></li>
    </ol>
  </nav>

  <div class="product-details">
    <h2><%= @product.name %></h2>
    <p><%= @product.description %></p>
    <p>
      Ex consequat commodo adipisicing exercitation aute excepteur occaecat ullamco duis aliqua id magna ullamco eu. Do aute ipsum ipsum ullamco cillum consectetur ut et aute consectetur labore. Fugiaolore proident.
    </p>
    <h4>Target Group</h4>
    <p><%= @product.target_group %></p>
        <p>
      Ex consequat commodo adipisicing exercitation aute excepteur occaecat ullamco duis aliqua id magna ullamco eu. Do aute ipsum ipsum ullamco cillum consectetur ut et aute consectetur labore. Fugiaolore proident.
    </p>
    <h4>About Publisher</h4>
    <p><%= @product.about_publisher %></p>
    Ex consequat commodo adipisicing exercitation aute excepteur occaecat ullamco duis aliqua id magna ullamco eu. 
    <h4>Price</h4>
    <p><%= @product.price %> $</p>
    <p>40</p>
    <h4>Start at:<%= @survey.start_date %> </h4>
    <h4>End at: <%= @survey.end_date %><h4>
    <button class="btn btn-secondary">Share</button>
  </div>

  <div class="survey-details">
    <%= link_to 'New Question', new_category_product_survey_question_path(@category, @product, @survey), class: 'btn btn-primary' %>

    <h3 class="mt-5">Questions</h3>
    <%= form_with(model: [@category, @product, @survey, Response.new], url: category_product_survey_responses_path(@category, @product, @survey)) do |form| %>
      <% @questions.each do |question| %>
        <div class="card question-card">
          <div class="card-body">
            <h5 class="card-title"><%= question.content %></h5>
            <% question.choices.each do |choice| %>
              <div class="form-check">
                <%= form.radio_button "responses[#{question.id}][choice_id]", choice.id, class: 'form-check-input' %>
                <%= form.label "responses_#{question.id}_#{choice.id}", choice.content, class: 'form-check-label' %>
                <%= form.hidden_field "responses[#{question.id}][question_id]", value: question.id %>
              </div>
            <% end %>
          </div>
          <div class="card-footer">
          <%= link_to 'Show', category_product_survey_question_path(@category, @product, @survey, question), class: 'btn btn-info' %>
          <%= link_to 'Edit', edit_category_product_survey_question_path(@category, @product, @survey, question), class: 'btn btn-warning' %>
          <%= link_to 'Destroy', category_product_survey_question_path(@category, @product, @survey, question), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
          </div>
        </div>
      <% end %>
      <div class="mt-3 text-center">
        <%= form.submit 'Submit your opinion', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
