<!-- app/views/products/_details.html.erb -->
<div class="card">
  <div class="card-body">
    <h5 class="card-title"><%= @product.name %></h5>
    <p class="card-text"><%= @product.description %></p>
    <%= link_to 'Express your opinion', new_category_product_survey_path(@category, @product), class: 'btn btn-primary' %>
    <%= link_to 'Share', '#', class: 'btn btn-secondary' %>
  </div>
</div>
